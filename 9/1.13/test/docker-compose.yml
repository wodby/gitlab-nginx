version: "2"

services:
  workhorse:
    image: wodby/gitlab:9.3
    command: /etc/init.d/workhorsed
  nginx:
    image: $IMAGE
    environment:
      NGINX_BACKEND_HOST: workhorse
      NGINX_BACKEND_PORT: 8181
    depends_on:
      - workhorse
